---
description: 审查暂存的 git 改动（git diff --cached）
---
审查 `git diff --cached` 的改动，重点关注：

1. **Bug 与逻辑错误**：边界条件、空值、循环/递归终止
2. **安全问题**：注入、权限校验、密钥泄露、不安全反序列化
3. **错误处理**：异常吞没、资源未释放、缺少回滚
4. **可维护性**：命名、复杂度、是否有更简单的实现

对每条问题给出：所在文件:行号、严重程度（高/中/低）、修改建议。
最后用一句话总结这次改动是否可以合入。
